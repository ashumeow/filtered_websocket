###
# An example config file, passed in the with --config or -c option
###
{
    "port": "9000",
    "redis_host": "localhost",
    "redis_port": "6379",
    "filters": ["filtered_websocket.filters.token_broadcast_filters", "filtered_websocket.filters.stdout_messages"]
}
