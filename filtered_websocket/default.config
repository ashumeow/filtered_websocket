###
# An example config file, passed in the with --config or -c option
###
{
    "port": "9000",
    "redis_host": "localhost",
    "redis_port": "6379",
    "filters": ["filters.token_broadcast_filters", "filters.stdout_messages"]
}
